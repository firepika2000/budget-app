# v0.4 Core-Completeness Acceptance Specification

Companion to [ynab-core-parity-2026.md](ynab-core-parity-2026.md). Defines what "core complete" means for the live, authenticated product and the prioritized backlog to get there. Branch: `codex/v0.4.0-stabilization` @ `43a5212`.

**Verdict:** **NOT CORE COMPLETE** — three P1 gaps remain (in-app targets, scheduled transactions, editable target progress). The fourth, the **account register**, was resolved by Codex `f93dce2` during this review (`LiveAccountRegisterView`); pending Mac runtime confirmation. The financial engine, permissions, and analytics are strong and in several areas exceed the baseline; the remaining gaps are client-surface completeness, not correctness.

Ownership note: the P1 items require SwiftUI in files Codex is actively editing (`BudgetWorkspaceView.swift`, `EditingViews.swift`, `APIClient.swift`). Per the parallel-work policy, **Claude owns the backend contracts, authorization, and regression tests** for each; **Codex owns the SwiftUI**. This doc specifies acceptance so both can work without conflict.

---

## Definition of "core complete"
A live user can, end-to-end (discover → use → persist → reload → propagate):
1. Open any account and see its **transaction register** with running/cleared balances, and add/edit within it.
2. Create/edit a **target** on a category and watch progress + underfunded + Auto-Assign respond.
3. Create a **scheduled/recurring** transaction and see it in the forecast and materialize.
4. Enter/edit/delete/split/transfer transactions and see all derived state update (already met).
5. Reconcile accounts (already met).
6. Use credit cards with correct reserve accounting (already met).
7. Explore reports with ranges/filters and drill to transactions, editing in place (already met; donut visualization to restore).
8. Operate the household/delegation/approval system (already met; exceeds baseline).

---

## P1 — Core Completeness (viability-blocking)

### P1-A — Account register / account-scoped history  *(RESOLVED — Codex `f93dce2`)*
- **Delivered:** `LiveAccountRegisterView` — that account's transactions (newest first), header with Working/Cleared/Uncleared balances + last-reconciled + payment reserve, add-transaction scoped to the account (`initialAccountID`), tap-through to edit, and Reconcile relocated into it. Reviewed: PASS (no new money math; register filters the already-server-scoped transaction list).
- **Claude (non-conflicting, still valuable):** add a backend privacy regression asserting a scoped member's `GET /transactions` returns only permitted accounts' rows (so the register cannot leak). No new endpoint required.
- **Acceptance (Mac):** Open account → list matches that account's transactions; balances correct; add/edit reflects immediately; scoped members can't open hidden accounts.

### P1-B — Targets: create/edit in-app  *(MISSING client; backend contract LOCKED)*
- **Now:** `PUT/GET/DELETE /categories/{id}/target` exist; **contract locked and fully tested** — see [targets-api-contract.md](targets-api-contract.md) and `test_targets_contract.py` (10 tests). Missing only the `APIClient` methods + editor UI.
- **Deliver (Codex):** `APIClient.upsertTarget/getTarget/deleteTarget` against the locked contract; a category target editor (type, amount, minimum, due date, recurrence, active toggle); progress bar on the category row from `recommended_contribution`/`underfunded`.
- **Claude (DONE this workstream):** authorization (manage_planning + scope, deny-by-default, hidden-category 404, cross-budget 404, deactivated member), accounting invariants (create/edit/delete never move money; recommendation ≠ assignment; future target adds no spendable cash), delete semantics, validation, and deterministic-LWW concurrency — all covered.
- **Caveats for Codex:** weekly cadence and per-month snooze are deferred (model is month-granular; `is_active` gives on/off); target upsert is last-writer-wins (no version) — safe because non-monetary.
- **Acceptance:** Set a target → persists+reloads → Plan shows progress and underfunded → Auto-Assign uses it.

### P1-C — Scheduled / recurring transactions in-app  *(MISSING client)*
- **Now:** `GET/POST /scheduled-transactions` exists; forecast shows occurrences; no create/list UI.
- **Deliver (Codex):** `APIClient` methods; a schedule list + editor (amount, account, category, cadence, next date).
- **Claude:** backend tests — scheduled entries feed forecast occurrences deterministically; scope enforcement; no scheduled entry mutates actuals until its date.
- **Acceptance:** Create a schedule → appears in forecast → materializes as a real transaction on its date (or via explicit "enter now").

### P1-D — Editable target progress  *(PARTIAL → depends on P1-B)*
- Fold into P1-B acceptance: the progress indicator must be tied to an editable target, not a read-only value.

---

## P2 — Completeness

- **P2-1 Restore Spending donut (Totals)** *(REGRESSED)* — add `SectorMark` donut with legend + % beside the existing bars; keep VoiceOver summary. (Codex SwiftUI.)
- **P2-2 Hide Amounts** *(REGRESSED)* — store flag + `format` mask + toolbar toggle. (Codex SwiftUI.)
- **P2-3 Category group management** — rename/reorder/delete groups; **P2-4 category reorder** (`sort_order`). Claude: backend ordering tests.
- **P2-5 Money-move history view** — surface `GET /allocations`. Claude: serialization/permission test.
- **P2-6 Transaction filters/sort** in register (account/flag/cleared/date) + server paging. Claude: server filter tests (extend analytics-style coverage to the register endpoint if added).
- **P2-7 First-class payees** (table, rename, merge) — schema + endpoints (Claude can own backend) then UI (Codex).
- **P2-8 Net Worth report** — aggregation endpoint (Claude) + trend UI (Codex).
- **P2-9 Cleared/uncleared/working balances** shown per account (part of P1-A header).
- **P2-10 Loan metadata + payoff** — persisted APR/term + payoff view.
- **P2-11 Spending Trends** multi-month series + trendline.
- **P2-12 Focused/custom Plan views** persisted.
- **P2-13 Attachments** binary storage endpoint + picker.
- **P2-14 Offline mutation queue** (DEFERRED-leaning P2).

## P3 — Polish / convenience
Age of Money; category templates; pinned/favorite categories; payee categorization suggestions (deterministic, never silent); close/reopen account action; in-app export; request-cancel UI (`cancelFinancialRequest` exists, no entry point); reimbursement first-class pattern; snooze target.

## Intentionally deferred (blocked)
Bank import, Apple Card import, bank OAuth, Plaid/MX/Finicity/Akoya/Teller, direct institution credentials, screen scraping, widgets. No work until separately approved.

---

## Claude's isolated, non-conflicting workstream (safe to implement now)
These touch **no** Codex-active SwiftUI files — backend/tests/docs only, each EDIT → TEST → COMMIT → PUSH:
1. **Money-conservation property tests** across every mutation (Σ postings == 0; total cash conserved).
2. **Account-register privacy tests** (P1-A backend precondition): a scoped member's transaction/balance queries never reveal hidden accounts.
3. **Target authorization + accounting tests** (P1-B backend precondition).
4. **Scheduled-transaction forecast/scope tests** (P1-C backend precondition).
5. **Net Worth aggregation** endpoint + tests (P2-8 backend), if assigned.

These lock the server contracts before Codex wires the SwiftUI, so the two streams converge without touching the same files.

## Core product principles (must remain true)
Exact Int64 minor units · account = location / category = purpose · future income never spendable now · on-budget transfers create no income/expense · credit-card reserve accounting · delegated authority never duplicates money · server-side permission enforcement · append-only/auditable money moves · recommendation-before-mutation for Auto-Assign · scenarios never mutate actuals until applied.
