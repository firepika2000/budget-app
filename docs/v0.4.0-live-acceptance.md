# v0.4.0 — Live PostgreSQL Simulator Acceptance (current)

Concrete findings and evidence from hands-on human acceptance of the **live** product:
the iOS Simulator connected to a **fresh PostgreSQL-backed Budget Server**, exercising the
real authenticated path rather than the deterministic demo. This document is the current
findings log; [ROADMAP.md](ROADMAP.md) remains the authoritative product direction and gate.
Companion procedural script: [v0.4.0 Mac/Xcode acceptance](v0.4.0-mac-acceptance.md).

Branch: `codex/v0.4.0-stabilization`. Session recorded 2026-09-07.

> **Verdict: v0.4 NOT accepted.** The end-to-end live path now runs far enough to reach an
> authenticated live application and create a first Budget, but a fresh household still cannot
> progress into normal budgeting from the Plan UI without an API/manual workaround (Finding 1).
> Core live financial-persistence acceptance is therefore blocked, not passed.

## Live path demonstrated

```
iOS Simulator → Live Budget Server → /api/v1/health → /api/v1/bootstrap/status
  → first-owner initialization → Sign In → authenticated live application
  → create first Budget → production data hydration
```

### PASS (observed working against authoritative state)

- Live server connection
- `GET /api/v1/health`
- `GET /api/v1/bootstrap/status`
- Automatic first-time-setup detection (uninitialized server → First-Time Setup)
- Backend first-owner bootstrap
- Initialized-server detection (initialized server → Sign In)
- Normal Sign In
- Authenticated live application entry
- First live Budget creation (owner had a working `+` action)
- Initial live Budget hydration
- Live reads for categories, groups, accounts, transactions, month state, reports,
  allocations, scheduled transactions, requests, allowances, forecast, and household members

## Findings

### Finding 1 — Fresh Budget Plan dead end *(v0.4 acceptance BLOCKER)*

A newly created live Budget contains no category groups or categories. The Plan screen
correctly renders the empty financial state but exposes **no discoverable action** to create
the first category group/category. The Focus menu contains only filters — All, Underfunded,
Overspent, Funded, Available — and no category management.

**Impact:** a brand-new live household can create a Budget but cannot progress into normal
budgeting from the Plan UI without an external/API workaround. This violates the
production-completeness and human-acceptance gates.

**Classification:** v0.4 acceptance blocker. The Plan empty state must provide a discoverable
path to create the first category group/category (see the v0.4 milestone and First-Budget
Experience requirement in the roadmap). This is a functional first-run onboarding gap, not the
polished guided tour.

**Engineering resolution — fixed, human retest pending.** `LivePlanView` now renders a "Build
your first plan" empty state (headline, explanation, and a prominent **Create Category Group**
primary action) whenever a loaded Budget has zero groups and zero categories and the user holds
`manage_budget_structure`. The action opens the existing production `CategoryCreationView` sheet —
which, with no groups present, creates the first group and first category together through the live
`createCategoryGroup`/`createCategory` API — so no onboarding-only category engine was introduced.
After creation the normal grouped Plan renders and the `+` toolbar menu remains the discoverable
path for further groups/categories. (Note: the `+` "Add category" menu already existed and is
shown to owners; the defect was discoverability on an empty screen, which the empty state fixes.)

### Finding 2 — Owner-specific empty-state wording *(v0.4 UX defect)*

Immediately after owner authentication, the Budgets empty state displayed wording equivalent
to *"No shared budgets / Ask the household owner to share a budget with this account."* The
authenticated user **was** the household owner, and did have a working `+` action and
successfully created a Budget — so functionality exists, but the copy is contextually
incorrect and confusing for an owner during fresh-budget onboarding.

**Classification:** UX defect to correct during fresh-budget onboarding closure. The empty
state should distinguish an owner (who can create a Budget) from a member awaiting a shared
Budget.

**Engineering resolution — fixed, human retest pending.** `BudgetListView`'s zero-Budget state is
now capability-aware. A user who owns an active household (`canCreateBudget`, the same signal that
gates the `+` action) sees *"No budgets yet / Create your first budget to start organizing your
household's money."* with a **Create Budget** action; a member without that authority still sees the
shared-budget guidance. Behavior is driven by the existing household-ownership signal, not a new
hard-coded role check.

### Finding 3 — iOS first-time bootstrap submission returned 422 *(defect requiring diagnosis)*

The first bootstrap attempts from the iOS setup form returned `422 Unprocessable Entity`.
A direct request built from the documented backend contract succeeded with `201 Created`.

Backend bootstrap requires: `email`, `password` (minimum 12 characters), `display_name`,
`household_name`. The iOS form **did** contain a Household Name field, so this is **not** a
simple missing-household-field defect.

**Root cause — identified (was unknown at first report).** The iOS request encoding is **correct**:
`BootstrapRequest` maps `display_name`/`household_name` via `CodingKeys`, verified by a new encoding
test. The request is not a DTO/mapping defect. The 422 was a **client-side validation gap**: the
setup form enforced only non-empty fields, so a password shorter than the backend's 12-character
minimum was submitted. Confirmed empirically against the live app — an 11-character password returns
`422` with a FastAPI validation **array** (`detail: [{loc, msg}]`, msg "String should have at least
12 characters"), while a valid password returns `201`. Two compounding issues: (a) no pre-submission
length check, and (b) the client's error decoder handled only string/object `detail`, not the 422
array, so the error surfaced as a generic "unprocessable" string.

**Engineering resolution — fixed, human retest pending.** The setup form now enforces the contract
before submission (password ≥ 12 with inline guidance, email length ≥ 3, non-blank display/household
names) and keeps the primary action disabled until a request would validate. The shared client's
`APIErrorBody` now also parses the FastAPI 422 array into a readable, field-labelled message
(e.g. *"Password: String should have at least 12 characters"*) without exposing raw Pydantic
`type`/`loc` internals. A valid iOS submission reaches `201` and authenticates with no curl/API-docs
workaround.

### Finding 4 — `GET /delegated-budgets/me` returned 404 for the owner *(semantic review)*

During initial hydration, `GET …/delegated-budgets/me` returned `404 Not Found` while the
authenticated user was the household owner. Other hydration calls succeeded.

This may be legitimate API semantics ("this user has no delegated budget") or an
integration/empty-state issue. Recorded as a **finding requiring review**, not a declared bug.
Related open item: [v0.4.0 Mac acceptance](v0.4.0-mac-acceptance.md) O-1 (whether a failed
`requests` / `/delegated-budgets/me` fetch should surface an error rather than silently
degrade).

**Review conclusion — confirmed intentional behavior; client already handles it cleanly. No change
required.** `GET /delegated-budgets/me` returns `404 "Delegated budget not found"` by design when
the caller has no delegated budget (an owner never does), while the collection `GET
/delegated-budgets` returns `200` — confirmed empirically (`404` for `/me`, `200` for the list). The
iOS workspace already loads `/me` with `try?`, so a `404` resolves to `delegatedBudget == nil`, which
renders the normal owner "Available to assign" Plan rather than any error or corrupted state. The
API contract and delegated-authority semantics are unchanged; a client test now locks that a `404`
surfaces as `server(404)` (→ nil) rather than a decode failure or a swallowed success. This is a
distinct concern from Mac O-1, which is about whether other silently-degraded fetches should instead
show an error — that remains a separate UX judgment call, not a correctness bug here.

### Finding 5 — Authentication refresh concurrency / session recovery *(defect)*

During live relaunch, the app displayed a *"Something went wrong / Invalid or expired refresh token"*
alert while still showing the authenticated Budgets UI. The server log for that relaunch (summarized)
was:

```
POST /api/v1/auth/refresh  -> 200
POST /api/v1/auth/refresh  -> 401
POST /api/v1/auth/refresh  -> 401
GET  /api/v1/me            -> 200
GET  /api/v1/budgets       -> 200
```

So the original refresh token was valid, one refresh succeeded and its new access token was usable
(`/me` and `/budgets` returned `200`), and additional concurrent refreshes used the now-rotated token
and failed — the losing `401` surfaced a user-visible error even though authentication had already
recovered. This is a refresh-token rotation race.

**Root cause.** `AppSession.refreshIfNeeded` read the stored refresh token and only wrote the rotated
token *after* the network `await`. On relaunch, multiple independent session-initialization paths —
`loadBudgets()` reached via `configureServer`/`validateSelectedSource` (both `RootView.task` and the
`scenePhase == .active` handler) plus `BudgetListView.task`, and direct `refreshIfNeeded` calls in
the workspace/detail views — each ran that read-then-write concurrently, so each submitted the same
`R1`. The backend rotated `R1→R2` for the winner (`200`) and rejected the stale `R1` for the losers
(`401`), and a loser's `401` propagated to `errorMessage`.

**First engineering fix (`e17c100`) — single-flight + generation-safe refresh.** Refresh was made
single-flight and generation-safe:

- At most one `/auth/refresh` is in flight per session; concurrent callers await the same shared task
  and receive the same rotated tokens. The check-and-install of the shared task runs synchronously on
  the main actor, so no second caller ever submits the old refresh token.
- Credential writes are serialized and carry a monotonic generation; a refresh result — success or
  failure — is applied only while its generation is still current, so a stale success cannot clobber
  newer tokens and a stale failure cannot clear credentials a newer refresh installed. This is
  enforced by generation, not by inspecting error strings.
- A recovered refresh no longer surfaces any user-facing error.
- A genuinely invalid/expired refresh token (with no competing success) clears credentials and
  transitions cleanly to Sign In, preserving the Live Budget Server selection and never falling back
  to the deterministic demo.

Automated coverage was added in the app test target (`ios/BudgetAppTests/AppSessionRefreshTests.swift`):
concurrent refresh issues exactly one request and keeps the rotated credentials with no error;
concurrent `loadBudgets` at startup issues one refresh and lands Connected; a genuine `401` clears to
Sign In while preserving Live server selection; and a stale success arriving after sign-out does not
re-authenticate. The transport is a deterministic gated mock (no sleeps).

**Mac human retest of `e17c100` — FAILED (not PASS).** Built and run on an iPhone 17 Pro Max
Simulator (iOS 27.0), on a launch whose stored refresh token had already been invalidated by the
earlier pre-fix race (so this exercised the genuine-invalid recovery path). The app again showed
*"Something went wrong / Invalid or expired refresh token"* over a stale authenticated-looking Budgets
screen. The server log for this launch was:

```
GET  /api/v1/health          -> 200
GET  /api/v1/health          -> 200
GET  /api/v1/bootstrap/status -> 200
GET  /api/v1/bootstrap/status -> 200
POST /api/v1/auth/refresh    -> 401
POST /api/v1/auth/refresh    -> 401
POST /api/v1/auth/refresh    -> 401
```

Three sequential refresh `401`s and duplicated discovery — a *failed invalid-session recovery*, distinct
from the original rotation race (do not conflate the two log blocks). `e17c100`'s single-flight only
collapses callers overlapping the in-flight `refreshTask` window; it did not make invalidation a
terminal, authoritative state, and it left two startup weaknesses that multiplied the load.

**Root cause of the recovery failure.** Refresh eligibility was gated only by the mutable
`refreshToken`/access-token freshness, with no authoritative "session invalidated" latch. Two
independent startup entry points — `RootView`'s `.task` and its `scenePhase == .active` handler —
each ran `validateSelectedSource → configureServer → (health, bootstrap) → loadBudgets → refreshIfNeeded`
(hence the duplicated `/health` and `/bootstrap/status`). Worse, `configureServer` published
`connectionStatus = .connected` *before* the authenticated load resolved, so `RootView` rendered
`BudgetListView` in the not-yet-loaded window — showing stale content and spawning that view's own
`loadBudgets → refreshIfNeeded`. Because these loaders were staggered behind different `await` chains
and eligibility was re-evaluated across the credential-clearing boundary, more than one `/auth/refresh`
went out for the already-invalid token, and the invalid session was not presented as Sign In.

**Second engineering fix (this task) — make invalid-session recovery terminal.**

- **Authoritative terminal latch (`authInvalidated`).** A current-generation refresh `401` latches the
  session invalid; `refreshIfNeeded` is then a no-op until a successful login/bootstrap/invitation
  resets it (in `save`). Eligibility is now "Live, not invalidated, has credentials" — a caller
  arriving after the failed refresh cannot begin a new refresh for the dead generation. This is a
  state gate, not a delay/retry-count/error-string check.
- **No premature `.connected`.** `configureServer` stays `.connecting` until `loadBudgets` resolves,
  so the Budgets UI (and its loader) is never presented during the authenticated-but-not-yet-loaded
  window. `loadBudgets` publishes `.connected` on success or `.authenticationRequired` on an invalid
  refresh, so a failed recovery routes straight to Sign In with no stale Budgets.
- **Coalesced startup validation.** `validateSelectedSource` is single-flight, so the `.task` and
  `scenePhase` entry points share one discovery + one authenticated load (removing the duplicate
  `/health` and `/bootstrap/status`).
- **401 is an auth transition, not an error.** The handled refresh `401` is consumed at the session
  boundary and never sets `errorMessage`; stale `budgets`/`profile` are cleared; Live server
  URL/source are preserved; no demo fallback. Other network/server errors still surface normally.

New regression coverage (same test file): sequential post-failure callers issue exactly one refresh and
stay invalidated with cleared budgets and no error; the concurrent startup composition (two
`validateSelectedSource` entry points) runs discovery and refresh exactly once — a test that would have
failed on `e17c100`'s duplicated discovery; and a successful login after invalidation restores a
refreshable session.

**Mac human retest of `f704e6e` — FAILED after Clean Build Folder.** The real iPhone 17 Pro Max
Simulator launch still produced two health requests, two bootstrap-status requests, three refresh
`401`s, stale Budgets behind the authentication failure, and the generic *Invalid or expired refresh
token* alert. This result supersedes the earlier source-level conclusion; `f704e6e` did not fix the
runtime defect.

**Third engineering correction — human retest pending.** Production startup now has one lifecycle
owner (`RootView.task(id: scenePhase)`) instead of independent `.task` and `scenePhase` callbacks.
The application composition root explicitly reuses one process-stable `AppSession`. Authenticated
loads capture their credential generation and discard both results and errors after invalidation, so
a suspended request cannot repopulate Budgets, restore Connected, or place an obsolete alert over
Sign In. The shared refresh task now includes installation/invalidation of credential state, rather
than representing only the HTTP request, so successful-rotation joiners cannot resume with the old
access token before the owner advances the generation. DEBUG diagnostics identify the build, stable session instance, caller, generation,
invalidation latch, refresh creation/join/completion, and connection state without logging secrets.

**Status: Engineering fixed / human retest pending.**

### Finding 6 — SwiftUI "Publishing changes from within view updates" warning *(separate; deferred)*

During the same human test, Xcode logged: *"Publishing changes from within view updates is not
allowed, this will cause undefined behavior."* This is a synchronous `@Published` mutation during a
SwiftUI view-update pass. It is **not** the same defect as Finding 5 — the refresh race sets state
from async continuations *after* an `await`, not within a view update — and it was not conclusively
attributed to the refresh path here. The invalid-session-recovery correction removed one synchronous
startup state publish (the premature `.connected` in `configureServer`), which may incidentally reduce
startup publishing, but the audit did **not** conclusively tie the warning to the authentication/session
sequencing, so it was not treated as the same root cause. **Status: recorded, deferred to a separate
UI-stabilization task.** It should be reproduced on a Mac (Xcode purple-runtime breakpoint) to capture
the exact publishing call site before fixing.

## Blocked acceptance

- **Live persistence test (`$720 → $860`) — still NOT run; the onboarding blocker is fixed in
  engineering, human retest pending.** The Finding 1 fix removes the dead end that prevented reaching
  categories, so the create → assign → persist → reload check is now expected to be *possible* from
  the UI. It has **not** been executed and must **not** be reported as passed until a human runs it
  on a Mac against a fresh server.
- Remaining live persistence and financial-workflow acceptance (assign, move money, record a
  transaction, watch category balances respond, relaunch persistence) is now unblocked in
  engineering and awaits human retest.

## Engineering closure pass (2026-09-07)

A fresh-budget onboarding closure pass landed on `codex/v0.4.0-stabilization` addressing Findings 1
and 2 (fixed, human retest pending), Finding 3 (root cause identified and fixed, human retest
pending), and Finding 4 (confirmed intentional, no change). Scope was UX/onboarding and client error
handling only — no financial invariants, engine behavior, migrations, or delegated-authority
semantics were changed. New automated coverage was added to the Swift `BudgetAPITests` package
(bootstrap DTO field-name encoding, 422 validation-array → readable message, delegated `/me` 404
contract).

**Native validation status: PENDING.** This closure pass was authored on a Windows host with no
Swift/Xcode toolchain, so the Swift package tests, the iOS unit tests, the Xcode build, and the
Simulator run were **not** executed here and native validation is **not** claimed. The backend was
not modified; the two backend behaviors these fixes depend on (bootstrap password `min_length=12` →
`422`; delegated `/me` → `404`) were reconfirmed against the live app on this host. Human acceptance
remains **not PASS**, and v0.4 remains **not complete**, until a Mac build/Simulator retest of the
human-acceptance target sequence succeeds.

## Engineering constraint (temporary)

GitHub Actions minutes for the repository are exhausted for the current month. Until CI
capacity returns, local verification is temporarily serving as the active gate. This is an
engineering constraint, not a product feature, and does not change any acceptance gate.

## Fresh-budget activation closure (2026-09-08)

The production iOS path now exposes the complete first-run sequence without introducing a separate
onboarding state machine. The UI derives every step from authoritative workspace data after refresh:

1. **Accounts → Add Account.** The empty Accounts screen explains what to add and exposes the same
   production account editor used by the populated toolbar. The editor accepts an exact decimal
   current balance and sends integer minor units. The server atomically records a cleared,
   uncategorized `Starting Balance` transaction with the account; on-budget cash therefore becomes
   Available to Assign through the existing ledger calculation. Tracking balances and credit debt do
   not become spendable income.
2. **Plan → Create Category Group → Add Category.** Each empty state is driven by the refreshed group
   and category collections and uses the existing production category-group/category APIs.
3. **Plan → category → Assign money.** Explanatory copy defines Available to Assign and points the user
   into the existing assignment editor and allocation ledger. Assignment remains server-authoritative
   and exact in minor units.

Structural actions remain gated by `manage_budget_structure`. A user without that capability sees
scope-appropriate explanatory copy and no unauthorized CTA. The budget workspace is now presented as
its own full-screen navigation root from the Budgets list, fixing the nested-navigation condition that
hid Accounts/Plan titles and toolbar actions on current SwiftUI releases. Demo and live still render
the same workspace views and store operations; demo is pre-populated and therefore does not naturally
display these fresh-data states.

**Engineering status:** fixed; automated verification recorded with the implementation checkpoint.
**Human status:** Simulator/live-server persistence acceptance remains required. Follow the fresh-server
steps below and do not mark v0.4 accepted until the created account, group, category, starting balance,
and assignment survive app termination and relaunch.

### Exact Mac Simulator acceptance steps

1. Start an isolated, empty Budget server database and launch the current iOS target in an iPhone
   simulator. Bootstrap/sign in, create a Budget, then open it from **Budgets**.
2. Open **Accounts**. Confirm the title and `+` are visible and the empty state says **Add your first
   account**. Tap **Add Account**, create an on-budget checking account named `Checking` with current
   balance `$720.00`, and confirm the register shows `$720.00` plus one cleared `Starting Balance` row.
3. Open **Plan**. Confirm Available to Assign is `$720.00`, tap **Create Category Group**, create
   `Monthly Bills`, then tap **Add Category** and create `Rent` in that group.
4. Open `Rent`, tap **Assign money**, assign `$300.00`, and save. Confirm Rent Available is `$300.00`
   and Available to Assign is `$420.00`.
5. Return to **Budgets**, reopen the Budget, pull to refresh, then terminate and relaunch the app.
   Confirm the account, `$720.00` balance, group, category, `$300.00` assignment, and `$420.00`
   Available to Assign persist.
6. Repeat with a view/contribute-only member. Confirm the explanatory empty state is visible where
   applicable and no Add Account/Create Group action is offered without structural capability.

### Human retest of `7f362c8` — FAILED; runtime correction pending retest

On iPhone 17 Pro Max Simulator / iOS 27.0, a fresh authenticated live Budget opened Home correctly,
but switching to Accounts selected the tab while leaving a blank white content area. The workspace
Back control and tab bar remained, while the Accounts title, add toolbar item, empty-state explanation,
and Add Account CTA were absent. This supersedes the earlier engineering-only conclusion for
`7f362c8`; human acceptance did not pass.

The initial navigation-ownership diagnosis was incomplete. The reproducible Accounts-to-Plan failure
was caused by the DEBUG runtime-build identity badge being overlaid at the root's bottom-leading edge,
across the Home/Plan tab hit region. The badge was visually tiny and declared non-hit-testing, but on
iOS 27 its overlay geometry still prevented the Plan tab from accepting XCTest and Simulator taps.
Accounts sat outside that geometry and therefore continued to work. The selected-tab body and its
fresh-budget state were healthy; Plan had never actually become selected.

The diagnostic badge now occupies the top-leading safe region, outside the tab bar. The production
workspace hierarchy, per-tab navigation ownership, capability checks, repositories, editors, and
server mutations are unchanged.

Regression coverage now hosts the complete dismiss-enabled `BudgetWorkspaceView` with an authoritative
zero-account/zero-category snapshot, changes the real TabView from Accounts to Plan three times, and
requires the Plan tab's selected state, navigation title, first-category action, and global Profile
action on every pass. It also renders a live-shaped empty workspace store. The earlier regression
changed selection programmatically below `RootView`, so it bypassed the root diagnostic overlay and
could not detect a blocked physical tab hit target.

**Status: engineering corrected; human live-server retest pending.**

### Human Live persistence pass and account/empty-group follow-up

Human testing on the Live provider passed fresh-budget account creation with a `$2,000.00` opening
balance, `$500.00` assignment, `-$100.00` uncleared categorized purchase, exact account/Plan
recalculation, and full reconstruction after terminating and relaunching the iOS process while the
server remained running. This is the first recorded human pass for Live persistence across app
termination. These accounting semantics are unchanged by the follow-up correction.

Two product gaps were found. Account creation's existing `is_on_budget` choice was labeled only
“Include in budget,” and accounts had no metadata editor. The canonical creation UI now calls this
**Budget treatment**, explains On budget versus Tracking, and restricts types to compatible families.
Account Settings edits name and safe same-family type metadata through the application service and
provider repository. Budget treatment is immutable after creation, and no balance field exists in
settings. The server independently rejects financially reclassifying type transitions. Credit-card
renames also keep their linked payment-category name aligned without changing reserve history.

The Plan previously rendered a category-group section only when filtered month rows existed, so a
provider-confirmed empty group disappeared. Every actual empty group now renders its name, “No
categories yet,” and an Add Category action that opens the canonical editor preselected to that group.
No placeholder group is synthesized. Existing Manage Groups behavior already supports rename,
ordering, hide/archive, and deletion of empty groups; deletion of groups containing categories remains
correctly rejected.

**Status: engineering corrected; account metadata and empty-group human Live retest pending.**

### Human account pass and populated category-group creation failure

Human testing of `c9617aa` passed renaming Test Checking to Primary Checking, changing its safe type
from Checking to Savings, and preserving the exact `$1,900.00` balance and `$1,500.00` Available to
Assign. A new `$10,000.00` Tracking account remained isolated from Available to Assign and the
90-day on-budget projection. Account metadata editing and tracking opening-balance isolation therefore
pass human acceptance; persistence across another relaunch remains to be confirmed separately.

The populated Plan failed category-group lifecycle acceptance: group creation was reachable only from
the first-plan onboarding state. Neither the populated Plan add menu nor Category Groups management
offered creation, and management exposed the raw zero-based `Order 0` implementation value.

The focused correction exposes the same canonical New Category Group editor from both the populated
Plan add menu and Category Groups management. It also removes raw numeric order controls from ordinary
UI while preserving each group's existing stored order during rename/hide edits. Newly created empty
groups continue to render in Plan with their own Add Category action.

**Status: engineering corrected; populated category-group lifecycle human retest pending.**

### Human contextual Add Category failure

Human testing of `70e0746` passed creation and visibility of an additional empty Savings Goals group,
including its empty state and Add Category action. Opening that contextual action incorrectly showed
Monthly Expenses as the selected group in New Category. The originating group and sheet visibility
were held in separate SwiftUI state values, allowing presentation to capture the prior/default group.

The focused correction represents category creation as one identifiable presentation value carrying
an optional originating group ID. Contextual actions carry their exact group; the global Plan action
carries no group and therefore retains the normal first-group default. Dismissal clears the entire
presentation value, preventing canceled contextual state from leaking into a later global editor.

**Status: engineering corrected; contextual category creation human retest pending.**

### Human contextual pass and duplicate category-name failure

Human testing of `afa7b91` passed contextual category creation for Savings Goals and Monthly
Expenses, verified that global creation did not inherit stale context, and confirmed persistence of
the existing group/category structure. The same pass found that Savings Goals could contain two
categories both named Emergency Fund.

The focused correction defines category-name identity within a group as the name after trimming
leading/trailing whitespace and case-folding. Create and rename now validate this consistently in the
editor, workspace boundary, deterministic provider, server, and database constraint. The same name
remains valid in different groups. Rejected operations do not alter financial observations.

Migration `0017_category_name_uniqueness` preserves any legacy duplicate rows exactly as stored by
leaving their normalized keys null. It backfills keys only for non-conflicting rows, then constrains
all new keyed writes. Existing duplicates are never deleted, merged, or silently renamed; they can be
cleaned up manually through the normal category lifecycle.

**Status: engineering corrected; normalized category-name uniqueness human retest pending.**

### Human retest of `4d65278` — FAILED; authentication and app-shell correction

On iPhone 17 Pro Max Simulator / iOS 27.0, relaunching a previously authenticated Live Server
session produced three rejected refresh requests, retained the stale Budgets presentation, and showed
the refresh-token server error in the generic alert. Server health and bootstrap discovery remained
healthy. This is an application authentication-ownership failure, not a server outage.

The production hierarchy still allowed authenticated work to begin at three presentation levels:
root source validation, the Budgets browser's loading tasks, and workspace reconstruction. The latter
became particularly visible once tab rematerialization correctly rebuilt selected workspace content.
Although `AppSession` serialized callers within one session, views still owned authentication entry
points and could race lifecycle reconstruction. Authentication is now exclusively initiated by the
process `AppSession` through the root scene lifecycle. Budget selection and workspace views consume an
already validated credential generation and never refresh it themselves. A rejected refresh therefore
clears Keychain credentials, profile, budgets, and active presentation once; retains server/source
configuration; and routes directly to Sign In without a generic alert or demo fallback.

The authenticated application now resolves a persisted active budget directly into the five-tab
workspace. The Budgets list is no longer the normal application root or a parent of a temporary
workspace. Profile & Settings provides current user/budget context, budget switching and creation,
household access, source/server status, and sign out. No budget presents guided first-budget creation;
multiple budgets without a valid saved selection present an explicit chooser. A created budget becomes
active immediately, and a saved selection is discarded if it is absent from the authoritative list.
Deterministic and Live sources both enter the same `ActiveBudgetShell` and production workspace views.

Regression coverage hosts the real `RootView` during a rejected refresh and asserts one request plus
terminal Sign In state, exercises valid rotation and stale-caller invalidation, verifies active-budget
selection/relaunch persistence, and locks deterministic/live routing to the same shell. Fresh Accounts
and Plan activation coverage remains in the full production workspace composition.

**Status: engineering corrected; human iOS 27 live-server retest pending.**

### Human auth-form retest of `4579278` and `cc88b5` — FAILED; input identity correction

After entering Deterministic Demo and switching back to the Live Budget Server, the connected Sign In
screen accepted the first Email character and then lost editing focus. Simulator keyboard/haptic service
messages observed alongside it were incidental; no haptics resource participates in this lifecycle.

The cause was the root scene validation modifier. It was keyed to `scenePhase` but attached to a root
whose conditional content changes during Demo → Connecting → Authentication Required. SwiftUI may
restart that task when the presentation branch is replaced even though the scene remains active. The
restarted validation published Connecting again, removed the Sign In hierarchy, and destroyed its
focused TextField immediately after the first edit. The form's values were also locally owned by that
disposable authentication view.

The first attempted correction in `cc88b5` moved `AuthenticationFormState` to a root-owned
`@StateObject`. Human acceptance proved that insufficient: each character published through that
observable object, invalidating the conditional root hierarchy and still destroying the focused field.
The test passed because it mutated the form model directly; it never used the Simulator keyboard and
therefore did not exercise SwiftUI focus continuity.

The architecture correction places `AuthenticationFormState` in a stable `AuthenticationFlowView`
below the application's route boundary. `RootView` does not observe individual keystrokes. `AppSession`
owns the once-per-active-scene validation latch, remains the sole credential refresh owner, and exposes
an explicit application route. Source switching may establish a new authentication flow, but typing
does not alter source, connection, authentication, active-budget, or root identity.

Regression coverage now includes a native XCUITest target. It launches the real production
authentication hierarchy on an iPhone 17 Pro Max / iOS 27 Simulator, taps the actual Email and Password
fields, types complete values through the software-keyboard path, and verifies the full email text,
password length, and stable authentication route. This closes the prior test-layer gap; it is automated
evidence and does not replace the required hands-on live-server retest.

**Status: architecture corrected and literal keyboard automation passed; final human continuous-keyboard
and live-server retest remains required.**

### Human retest of `99cf5ef` — keyboard/sign-in PASS; active-budget routing FAILED

On iPhone 17 Pro Max / iOS 27, continuous Email and Password entry passed and Live Sign In succeeded.
Immediately after authentication, the application incorrectly rendered the standalone **Budgets**
browser even though the authoritative server returned exactly one accessible budget, **Test budget**.
The tester did not tap that budget. This is a human acceptance failure: a one-budget household must
establish that budget as application context and enter `ActiveBudgetShell` directly.

The proven cause was a post-authentication hydration race in `AppSession.authenticate`. Saving the new
tokens published `token` while `connectionStatus` still represented the authentication screen and
before `/me` and `/budgets` completed. `route` therefore evaluated the session as authenticated with an
empty in-memory budget collection and exposed `BudgetSelectionView`. The active-budget resolver ran
later and was logically correct, but the production router had already exposed an invalid intermediate
state.

Authentication now remains in the explicit Connecting route until profile and budget hydration finish,
the sole/valid persisted budget is reconciled, and Connected is published. Automated production-root
coverage holds the authoritative budget response in flight, proves the Budgets route is never exposed,
then verifies the single budget becomes active and the route enters the workspace. Human confirmation
of the corrected Live transition remains required.

**Status: engineering corrected; human Live post-authentication retest pending.**

### Human retest of `7230b46` — architecture parity FAILED

Human acceptance on iPhone 17 Pro Max / iOS 27 confirmed continuous keyboard entry **PASS** and Live
authentication **PASS**, but post-authentication workspace routing **FAIL**. With exactly one accessible
budget named **Test budget**, Live still rendered the legacy-looking **Budgets** browser (Sign out,
create, refresh, and a chevron row) instead of the five-tab workspace. Demo/Live production parity is
therefore **FAIL** at this checkpoint; the automated result must not override the screenshot.

The architectural fork was concrete. `ApplicationRoute` had separate `deterministicWorkspace` and
`workspace(APIBudget)` cases. The former entered the demo constructor while Live could enter
`budgetSelection`. In addition, `ActiveBudgetShell` contained its own `activeBudget == nil` fallback to
`BudgetSelectionView`, creating a second route decision below `RootView`. Finally,
`BudgetWorkspaceStore` dispatched commands with concrete deterministic downcasts and direct Live API
calls.

The correction replaces those paths with one `workspace(WorkspaceRouteContext)` route and one
`ActiveBudgetShell`. Only `RootView` may select the zero-budget onboarding/multi-budget chooser; the
shell cannot render it. The resolved context is immutable for the shell lifetime, preventing an
outgoing Demo shell from rereading a partially transitioned Live session. A single composition root
injects either repository into the same store and `BudgetWorkspaceView`. Reads use the shared snapshot
contract and every mutation uses `WorkspaceCommandRepository`; source-specific persistence is confined
to repository implementations.

Engineering manually launched the built app in both modes on the iOS 27 Simulator. Deterministic
rendered `ActiveBudgetShell → BudgetWorkspaceView` with the five tabs and Rivera Household fixtures.
The persisted authenticated Live source with **Test budget** rendered the same shell/view and five tabs,
with Live `$0.00` data. This is engineering inspection only. Human Demo/Live and post-authentication
retest remains required.

**Status: architecture corrected and locally verified; human parity/post-authentication retest pending.**
